{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:approval:event:approval.step_completed:v1",
  "title": "Approval Step Completed",
  "description": "单步审批完成（sequential/parallel 分支合并）时触发",
  "type": "object",
  "required": ["metadata", "payload"],
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "type": "object",
      "description": "Event metadata conforming to D2-2 Event Metadata Standard (10-field fixed template).",
      "required": ["identity", "context"],
      "additionalProperties": false,
      "properties": {
        "identity": {
          "type": "object",
          "description": "Event identity fields.",
          "required": ["event_id", "event_type", "version"],
          "additionalProperties": false,
          "properties": {
            "event_id": {
              "type": "string",
              "format": "uuid",
              "description": "Globally unique identifier for the event instance."
            },
            "event_type": {
              "const": "approval.step_completed",
              "description": "Event type identifier."
            },
            "version": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$",
              "description": "Semantic version of the event schema."
            }
          }
        },
        "context": {
          "type": "object",
          "description": "Event context fields.",
          "required": ["timestamp", "source", "producer", "correlation_id", "causation_id", "trace_id", "tenant_id", "user_id"],
          "additionalProperties": false,
          "properties": {
            "timestamp": {
              "type": "string",
              "format": "date-time",
              "description": "ISO 8601 UTC timestamp when the event was produced."
            },
            "source": {
              "const": "approval",
              "description": "Origin system or service that produced the event."
            },
            "producer": {
              "type": "string",
              "description": "Specific component or service instance that produced the event."
            },
            "correlation_id": {
              "type": "string",
              "description": "Identifier used to correlate related events across multiple systems."
            },
            "causation_id": {
              "type": "string",
              "description": "Identifier of the event that caused this event (e.g., the parent command)."
            },
            "trace_id": {
              "type": "string",
              "description": "Distributed tracing correlation identifier."
            },
            "tenant_id": {
              "type": "string",
              "description": "Tenant or organization namespace the event belongs to."
            },
            "user_id": {
              "type": "string",
              "description": "Identifier of the user who triggered the event."
            }
          }
        }
      }
    },
    "payload": {
      "type": "object",
      "description": "Approval step completion data payload.",
      "required": ["request_id", "step_id", "decision", "approver_id", "completed_at"],
      "additionalProperties": false,
      "properties": {
        "request_id": {
          "type": "string",
          "description": "Unique identifier of the parent approval request."
        },
        "step_id": {
          "type": "string",
          "description": "Unique identifier of the completed step."
        },
        "step_order": {
          "type": "integer",
          "minimum": 1,
          "description": "Ordinal position of this step in the approval chain."
        },
        "step_type": {
          "type": "string",
          "enum": ["sequential", "parallel"],
          "description": "Type of this step: sequential or parallel."
        },
        "decision": {
          "type": "string",
          "enum": ["approved", "rejected"],
          "description": "Decision made by the approver."
        },
        "approver_id": {
          "type": "string",
          "description": "Identifier of the approver who made the decision."
        },
        "approver_name": {
          "type": "string",
          "description": "Display name of the approver."
        },
        "comment": {
          "type": "string",
          "description": "Optional comment provided by the approver."
        },
        "completed_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the step was completed."
        },
        "remaining_steps": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of steps remaining in the approval chain after this step."
        },
        "is_final_step": {
          "type": "boolean",
          "description": "Whether this is the last step in the approval chain."
        }
      }
    }
  }
}
