{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:project_management:event:project.status_changed:v1",
  "title": "Project Status Changed Event",
  "description": "Emitted when a project's status changes in the project_management system.",
  "type": "object",
  "required": ["metadata", "payload"],
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "type": "object",
      "description": "Event metadata conforming to D2-2 Event Metadata Standard (10-field fixed template).",
      "required": ["identity", "context"],
      "additionalProperties": false,
      "properties": {
        "identity": {
          "type": "object",
          "description": "Event identity fields.",
          "required": ["event_id", "event_type", "version"],
          "additionalProperties": false,
          "properties": {
            "event_id": {
              "type": "string",
              "format": "uuid",
              "description": "Globally unique identifier for the event instance."
            },
            "event_type": {
              "const": "project.status_changed",
              "description": "Event type identifier."
            },
            "version": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$",
              "description": "Semantic version of the event schema."
            }
          }
        },
        "context": {
          "type": "object",
          "description": "Event context fields.",
          "required": ["timestamp", "source", "producer", "correlation_id", "causation_id", "trace_id", "tenant_id", "user_id"],
          "additionalProperties": false,
          "properties": {
            "timestamp": {
              "type": "string",
              "format": "date-time",
              "description": "ISO 8601 UTC timestamp when the event was produced."
            },
            "source": {
              "const": "project_management",
              "description": "Origin system or service that produced the event."
            },
            "producer": {
              "type": "string",
              "description": "Specific component or service instance that produced the event."
            },
            "correlation_id": {
              "type": "string",
              "description": "Identifier used to correlate related events across multiple systems."
            },
            "causation_id": {
              "type": "string",
              "description": "Identifier of the event that caused this event (e.g., the parent command)."
            },
            "trace_id": {
              "type": "string",
              "description": "Distributed tracing correlation identifier."
            },
            "tenant_id": {
              "type": "string",
              "description": "Tenant or organization namespace the event belongs to."
            },
            "user_id": {
              "type": "string",
              "description": "Identifier of the user who triggered the event."
            }
          }
        }
      }
    },
    "payload": {
      "type": "object",
      "description": "Project status change data payload.",
      "required": ["project_id", "from_status", "to_status", "changed_by"],
      "additionalProperties": false,
      "properties": {
        "project_id": {
          "type": "string",
          "description": "Unique identifier of the project."
        },
        "from_status": {
          "type": "string",
          "enum": ["planning", "running", "completed", "paused", "cancelled"],
          "description": "Previous status of the project before the change."
        },
        "to_status": {
          "type": "string",
          "enum": ["planning", "running", "completed", "paused", "cancelled"],
          "description": "New status of the project after the change."
        },
        "changed_by": {
          "type": "string",
          "description": "Identifier of the user who changed the project status."
        },
        "reason": {
          "type": "string",
          "description": "Reason or justification for the status change."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the status change occurred."
        }
      }
    }
  }
}
