{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:purchases:event:purchase.order.approved:v1",
  "title": "Purchase Order Approved Event",
  "description": "Emitted when a purchase order is approved in the purchases system.",
  "type": "object",
  "required": ["metadata", "payload"],
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "type": "object",
      "description": "Event metadata conforming to D2-2 Event Metadata Standard (10-field fixed template).",
      "required": ["identity", "context"],
      "additionalProperties": false,
      "properties": {
        "identity": {
          "type": "object",
          "description": "Event identity fields.",
          "required": ["event_id", "event_type", "version"],
          "additionalProperties": false,
          "properties": {
            "event_id": {
              "type": "string",
              "format": "uuid",
              "description": "Globally unique identifier for the event instance."
            },
            "event_type": {
              "const": "purchase.order.approved",
              "description": "Event type identifier."
            },
            "version": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$",
              "description": "Semantic version of the event schema."
            }
          }
        },
        "context": {
          "type": "object",
          "description": "Event context fields.",
          "required": ["timestamp", "source", "producer", "correlation_id", "causation_id", "trace_id", "tenant_id", "user_id"],
          "additionalProperties": false,
          "properties": {
            "timestamp": {
              "type": "string",
              "format": "date-time",
              "description": "ISO 8601 UTC timestamp when the event was produced."
            },
            "source": {
              "const": "purchases",
              "description": "Origin system or service that produced the event."
            },
            "producer": {
              "type": "string",
              "description": "Specific component or service instance that produced the event."
            },
            "correlation_id": {
              "type": "string",
              "description": "Identifier used to correlate related events across multiple systems."
            },
            "causation_id": {
              "type": "string",
              "description": "Identifier of the event that caused this event (e.g., the parent command)."
            },
            "trace_id": {
              "type": "string",
              "description": "Distributed tracing correlation identifier."
            },
            "tenant_id": {
              "type": "string",
              "description": "Tenant or organization namespace the event belongs to."
            },
            "user_id": {
              "type": "string",
              "description": "Identifier of the user who triggered the event."
            }
          }
        }
      }
    },
    "payload": {
      "type": "object",
      "description": "Purchase order approval data payload.",
      "required": ["order_id", "order_no", "approved_by", "approved_at"],
      "additionalProperties": false,
      "properties": {
        "order_id": {
          "type": "string",
          "description": "Unique identifier of the purchase order."
        },
        "order_no": {
          "type": "string",
          "description": "Business order number of the purchase order."
        },
        "approved_by": {
          "type": "string",
          "description": "Identifier of the user who approved the purchase order."
        },
        "approved_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 UTC timestamp when the purchase order was approved."
        }
      }
    }
  }
}
