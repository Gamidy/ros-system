{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:approval:event:approval.requested:v1",
  "title": "Approval Requested",
  "description": "审批请求提交时触发",
  "type": "object",
  "required": ["metadata", "payload"],
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "type": "object",
      "description": "Event metadata conforming to D2-2 Event Metadata Standard (10-field fixed template).",
      "required": ["identity", "context"],
      "additionalProperties": false,
      "properties": {
        "identity": {
          "type": "object",
          "description": "Event identity fields.",
          "required": ["event_id", "event_type", "version"],
          "additionalProperties": false,
          "properties": {
            "event_id": {
              "type": "string",
              "format": "uuid",
              "description": "Globally unique identifier for the event instance."
            },
            "event_type": {
              "const": "approval.requested",
              "description": "Event type identifier."
            },
            "version": {
              "type": "string",
              "pattern": "^\\d+\\.\\d+\\.\\d+$",
              "description": "Semantic version of the event schema."
            }
          }
        },
        "context": {
          "type": "object",
          "description": "Event context fields.",
          "required": ["timestamp", "source", "producer", "correlation_id", "causation_id", "trace_id", "tenant_id", "user_id"],
          "additionalProperties": false,
          "properties": {
            "timestamp": {
              "type": "string",
              "format": "date-time",
              "description": "ISO 8601 UTC timestamp when the event was produced."
            },
            "source": {
              "const": "approval",
              "description": "Origin system or service that produced the event."
            },
            "producer": {
              "type": "string",
              "description": "Specific component or service instance that produced the event."
            },
            "correlation_id": {
              "type": "string",
              "description": "Identifier used to correlate related events across multiple systems."
            },
            "causation_id": {
              "type": "string",
              "description": "Identifier of the event that caused this event (e.g., the parent command)."
            },
            "trace_id": {
              "type": "string",
              "description": "Distributed tracing correlation identifier."
            },
            "tenant_id": {
              "type": "string",
              "description": "Tenant or organization namespace the event belongs to."
            },
            "user_id": {
              "type": "string",
              "description": "Identifier of the user who triggered the event."
            }
          }
        }
      }
    },
    "payload": {
      "type": "object",
      "description": "Approval request submission data payload.",
      "required": ["request_id", "requester_id", "chain_id", "title", "submitted_at"],
      "additionalProperties": false,
      "properties": {
        "request_id": {
          "type": "string",
          "description": "Unique identifier of the approval request."
        },
        "requester_id": {
          "type": "string",
          "description": "Identifier of the user who submitted the approval request."
        },
        "requester_name": {
          "type": "string",
          "description": "Display name of the requester."
        },
        "chain_id": {
          "type": "string",
          "description": "Identifier of the approval chain definition."
        },
        "chain_name": {
          "type": "string",
          "description": "Display name of the approval chain."
        },
        "approval_type": {
          "type": "string",
          "enum": ["sequential", "parallel"],
          "description": "Type of approval chain: sequential or parallel."
        },
        "title": {
          "type": "string",
          "description": "Title or subject of the approval request."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the approval request."
        },
        "urgency": {
          "type": "string",
          "enum": ["normal", "urgent"],
          "description": "Urgency level of the approval request."
        },
        "submitted_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the request was submitted."
        },
        "metadata": {
          "type": "object",
          "description": "Extended business metadata.",
          "required": [],
          "additionalProperties": false,
          "properties": {
            "business_type": {
              "type": "string",
              "description": "Type of business entity this request relates to."
            },
            "business_id": {
              "type": "string",
              "description": "Identifier of the business entity this request relates to."
            }
          }
        }
      }
    }
  }
}
