FROM ros-system-ros-backend:fixed
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir pymysql 2>/dev/null || true
ENV ENV=production
ENV PYTHONUNBUFFERED=1
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "2"]
